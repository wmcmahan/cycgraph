---
"@cycgraph/studio": patch
---

The playground now registers only the A2A scenario agents the scenario server currently advertises, so a machine with no model no longer gets an Agent Card resolution error when a model-backed scenario is invoked.
